# towrating.org — repo brain

Astro 5 static pSEO site: US vehicle towing capacities. Quality-gated build (`src/lib/quality-gate.ts`, `isIndexable` needs >=4/5 spec fields) — 1,216 indexable pages out of ~13k trims, sitemap auto-derived from built pages. This is the portfolio's model pSEO gate (see `pseo-demotion-prevention-jun13.md`, scored 24/30 Safe).

Deploy: push to GitHub (`sunnyp81/towrating`, branch `master`), then `npx wrangler pages deploy dist --project-name towrating` (Cloudflare Pages). DNS on `towrating.org`.

## 2026-07-07 — recovery pass (early ramp, zero clicks, 27->270 impressions)
- Diagnosis: not a demotion. GSC shows a single hub page `/ford/f-250/` (238 impr) and its `2020` year page pulling nearly all impressions, all long-tail "f250 towing capacity" query variants sitting at avg position 45-90 (page 5-9+). Zero clicks is expected at that position; this is a brand-new site still climbing, not a ranking loss. No indexing/technical demotion signal in GSC.
- Fixed hard-rule violation: site-wide em/en dash sweep, 153 em dashes + 106 en dashes removed across 21 src files (layouts, guides MDX, prose.ts template, capacity-utils.ts, data/capacity-bracket-index.json) plus llms.txt. Titles: em dash -> colon (e.g. "Ford F-250 Towing Capacity by Year: All Generations"). Prose: em dash -> comma. Numeric ranges: en dash -> hyphen ("7,501-10,000 lbs"). Verified 0 dashes left in `dist/` after rebuild, 1216 pages still built (quality gate unaffected).
- Fixed technical bug: `scripts/indexnow-ping.mjs` and `scripts/build-default-og.mjs` referenced the wrong domain `towrating.net` (site is `towrating.org`) — IndexNow pings were fetching/submitting the wrong host and silently doing nothing useful. Corrected to `towrating.org`.
- Did not touch: quality gate, page count, titles/metas beyond the em-dash-to-colon swap (no CTR rewrite pass), no new pages, no postcode-level pages.
- Next: keep building authority/backlinks to `/ford/f-250/` and other early-impression pages; no content fix will move position 45-90 to page 1 by itself at this traffic volume, this is a time/links problem not a content-quality problem.

## 2026-07-10 — affiliate hook verification (diagnostic only, no fix needed/possible)
- Curled live rendered pages (`/ford/f-250/2020/`, `/ford/f-250/2020/xlt-7-3l-godzilla-v8-4x4/`): CarGurus link renders with no `affiliate_id` param, Amazon hitch/brake-controller links render with no `tag` param. `npx wrangler pages secret list --project-name towrating` confirms zero secrets set on the CF Pages project (Git Provider: No, so it's a manual `wrangler pages deploy dist` build, not a CF dashboard build; no `.env` file exists locally either). This is the documented Phase 1 design ("wired with env-var placeholders", `docs/superpowers/specs/2026-04-26-towrating-design.md` line 189), not a bug: `src/lib/affiliate.ts` correctly falls back to the bare partner URL when its env var is unset, and the two live templates (`TrimPage.astro`, `YearPage.astro`) call the helpers correctly with `rel="sponsored noopener" target="_blank"`.
- No trivial wiring bug found: env var names match between `affiliate.ts` and the design spec, no wrong-var/unrendered-component bug on the CarGurus/Amazon paths.
- Gap found (separate from missing IDs): `trueCarUrl`, `eTrailerUrl`, `carShieldUrl`, `insuranceUrl` are defined in `affiliate.ts` but never called from any page/layout, planned monetization slots (insurance right rail, CarShield right rail, eTrailer on guide pages) were never actually placed in templates. Also, 4 Amazon search links hardcoded as raw URLs in guide MDX prose (`weight-distribution-hitches.mdx`, `gcwr-explained.mdx`, `tongue-weight-10-percent-rule.mdx`, `brake-controllers-explained.mdx`) bypass `affiliate.ts` entirely, so they will never carry `PUBLIC_AMAZON_TAG` even after it's set, unless someone updates them separately.
- Not fixed: no real affiliate IDs exist anywhere (repo, `.env`, CF Pages secrets) for any of the 6 programmes, so there was nothing to wire a real ID into. No code changed, no build/deploy run.
- Blocked on Sunny: sign up for CarGurus, TrueCar, Amazon Associates, eTrailer, CarShield and an insurance affiliate (MoneyGeek/Zebra/Jerry), then add the 6 `PUBLIC_*_AFF_ID`/`PUBLIC_AMAZON_TAG` values to a local `.env` (gitignored) before the next `npm run build && npx wrangler pages deploy dist --project-name towrating`.
